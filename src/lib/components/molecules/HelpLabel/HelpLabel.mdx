import { Canvas, Meta } from "@storybook/blocks";

import * as Stories from "./HelpLabel.stories";

<Meta of={Stories} />

# HelpLabel

<br />

A help label text molecule component for generating complex organisms.

## Usage

<Canvas of={Stories.Base} />

## Error

An error state can be shown via `error` prop.

<Canvas of={Stories.Error} />