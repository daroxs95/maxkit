import { Canvas, Meta } from "@storybook/blocks";

import * as Stories from "./Text.stories";

<Meta of={Stories} />

# Box

<br />

A text atom component for generating complex molecules.

## Usage

<Canvas of={Stories.Base} />

## Changing underlying element

Use the `as` prop to change the underlying element.

<Canvas of={Stories.BaseAs} />

## Responsive

Use the `lg` prop to set the text size for large screens.

<Canvas of={Stories.Responsive} />
